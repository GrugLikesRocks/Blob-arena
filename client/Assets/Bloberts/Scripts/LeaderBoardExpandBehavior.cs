using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class LeaderBoardExpandBehavior : Menu
{
    [SerializeField] GameObject wholeLeaderBoard;
    [SerializeField] GameObject focusPage ;

    [SerializeField] TMP_Text[] traitsText = new TMP_Text[5];



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
